version: 2.1

jobs:
  build:
    docker:
      - image: node:14

    working_directory: ~/app

    steps:
      - checkout

      - run:
          name: install mocha 
          command: npm install mocha

      - run:
          name: Run tests
          command: npm test

             

workflows:
  version: 2
  build-and-test:
    jobs:
      - build
